//import img from "../images/wow.jpg";
import { myFunc } from "./test.js";

// Vite automatically handles asset imports, no need for webpack's require.context
// Assets will be available when imported or referenced in CSS/HTML

console.log("Application loaded successfully!");

let a = "hello";

console.log(a);

myFunc();
